interface:
  display_name: "Herdr Orchestrator"
  short_description: "Coordinate work through Herdr worker sessions"
  default_prompt: "Use $herdr-orchestrator to coordinate this work through named Herdr worker sessions."
policy:
  allow_implicit_invocation: false
